models:
  - name: fct_events
    description: '{{ doc("table_events") }}'
    columns:
      - name: user_id
        description: '{{ doc("bingeflix_user_id") }}'
        meta:
          priority: high
        tests:
          - not_null
          - unique